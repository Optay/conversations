import { dateIso } from '../../../helpers/date-iso';
import { module, test } from 'qunit';

module('Unit | Helper | date iso');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateIso(42);
  assert.ok(result);
});
